# Replace with the real HTTPS origin, without a trailing path.
PUBLIC_URL=https://declaration.example.org
# Never commit a setup code or passwords to a repository.
# First-run setup code is generated automatically and printed in server logs.
